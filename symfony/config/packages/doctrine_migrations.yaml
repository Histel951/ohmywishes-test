doctrine_migrations:
    migrations_paths:
        # namespace is arbitrary but should be different from App\Migrations
        # as migrations classes should NOT be autoloaded
        'App\Wishlist\Infrastructure\Database\Migrations': '%kernel.project_dir%/src/Wishlist/Infrastructure/Database/Migrations'
    enable_profiler: false
